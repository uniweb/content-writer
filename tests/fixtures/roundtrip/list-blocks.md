# Blocks inside list items

- Use the hook:
  ```jsx
  const { data } = useEntityDetail(article)
  ```
- A quoted aside:
  > Worth knowing.
- A nested list:
  - child
  - sibling
- Plain item

1. Step one
   ```sh
   pnpm install
   ```
2. Step two
